import { PlaceholderPage } from "@/components/dashboard/placeholder-page";

export default function LogsPage() {
  return (
    <PlaceholderPage
      title="Logs"
      description="System activity log — sync runs, generation jobs, uploads, and errors."
      phase="Phase 12+"
    />
  );
}
