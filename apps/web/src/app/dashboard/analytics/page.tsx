import { PlaceholderPage } from "@/components/dashboard/placeholder-page";

export default function AnalyticsPage() {
  return (
    <PlaceholderPage
      title="Analytics"
      description="Reviews generated vs. uploaded, failed uploads, duplicates prevented, reviewer distribution, and rating distribution."
      phase="Phase 14"
    />
  );
}
