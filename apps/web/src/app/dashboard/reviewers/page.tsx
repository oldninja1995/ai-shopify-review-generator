import { PlaceholderPage } from "@/components/dashboard/placeholder-page";

export default function ReviewersPage() {
  return (
    <PlaceholderPage
      title="Reviewer Database"
      description="Manage generated reviewer profiles — gender mix, countries, age groups, occupations, and verified purchase ratios."
      phase="Phase 8"
    />
  );
}
