import { PlaceholderPage } from "@/components/dashboard/placeholder-page";

export default function ProductsPage() {
  return (
    <PlaceholderPage
      title="Products"
      description="Import and browse products, images, descriptions, collections, tags, and variants synced from your connected Shopify store."
      phase="Phase 4"
    />
  );
}
